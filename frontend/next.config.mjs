/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  // Transfer all your configuration options from the .ts file here
  reactStrictMode: true,
  // swcMinify: true,
  // Other options...
};

export default nextConfig;